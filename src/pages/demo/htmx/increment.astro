---
import { incrementCounter } from './_db';

export const prerender = false;

if (Astro.request.method !== 'POST') {
	return Astro.redirect('/demo/htmx');
}
const i = await incrementCounter();
---

{i}
